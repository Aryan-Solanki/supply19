class UserData {
  String email, name, phnum, verify, volid, image;
  int number_of_posts, points;
  UserData(this.email, this.name, this.phnum, this.verify, this.volid,
      this.number_of_posts, this.points, this.image);
}
