class QueryUiData {
  String date, description, image, location, name, phnum, requirement, time;
  int order, query_no;
  QueryUiData(
    this.date,
    this.description,
    this.image,
    this.location,
    this.name,
    this.order,
    this.phnum,
    this.query_no,
    this.requirement,
    this.time,
  );
}
